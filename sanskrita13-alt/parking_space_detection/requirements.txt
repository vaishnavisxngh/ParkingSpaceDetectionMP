# Parking Space Detection System — Dependencies
# Install with: pip install -r requirements.txt

# Core
opencv-python>=4.7.0
numpy>=1.24.0

# Deep Learning (PyTorch — CPU version; swap for CUDA below if needed)
torch>=2.0.0
torchvision>=0.15.0

# YOLOv5 via torch.hub (auto-downloaded on first run)
# No extra install needed — just torch.hub.load("ultralytics/yolov5", ...)

# Config & utilities
PyYAML>=6.0

# Optional: Streamlit dashboard
# streamlit>=1.30.0

# Optional: data analysis of logs
# pandas>=2.0.0

# ─────────────────────────────────────────────────
# For CUDA (GPU) support, replace the torch lines above with:
#   torch>=2.0.0+cu118
#   torchvision>=0.15.0+cu118
# And install from: https://pytorch.org/get-started/locally/
# ─────────────────────────────────────────────────
